ext.LANGUAGES_DIR_NAME = 'languages'
ext.DEFINITIONS_DIR_NAME = 'definitions'
ext.DICTIONARIES_DIR_NAME = 'dictionaries'

ext.LANGUAGES_DIR = "${project.rootDir}/${LANGUAGES_DIR_NAME}"
ext.DEFINITIONS_DIR = "${LANGUAGES_DIR}/${DEFINITIONS_DIR_NAME}"
ext.DICTIONARIES_DIR = "${LANGUAGES_DIR}/${DICTIONARIES_DIR_NAME}"

ext.MAX_CHARS_PER_KEY = 15
ext.COMPRESSED_DICTIONARY_EXTENSION = 't9d'